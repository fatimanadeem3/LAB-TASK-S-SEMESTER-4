to run this project you have to make .env file and get two api keys 
1-groq api key(for nlp) it is a free api key any you can use it without paying.
2-stability ai(for image) it is a free api key but only allow max 5-6 hits after that you have to pay for api.
